---
export interface Props {
  text: string;
  isH1?: boolean;
}

const { text, isH1 = false } = Astro.props;
const Tag = isH1 ? 'h1' : 'h2';
---

<Tag
  class:list={[
    'text-5xl md:text-6xl',
    'font-title',
    'text-brown',
    'mb-6',
  ]}
>
  {text}
</Tag> 